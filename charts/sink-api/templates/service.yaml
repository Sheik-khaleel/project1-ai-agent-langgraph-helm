apiVersion: v1
kind: Service
metadata:
  name: {{ include "sink-api.fullname" . }}
  labels:
    {{- include "sink-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sink-api.selectorLabels" . | nindent 4 }}
